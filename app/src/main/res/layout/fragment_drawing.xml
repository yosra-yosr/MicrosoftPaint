<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <TextView
        android:id="@+id/title_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Let's Paint"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <LinearLayout
        android:id="@+id/tools_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/title_text"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageButton
            android:id="@+id/brush_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_brush"
            android:background="@drawable/tool_selector"
            android:contentDescription="Pinceau"
            android:layout_margin="8dp" />

        <ImageButton
            android:id="@+id/pencil_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_pencil"
            android:background="@drawable/tool_selector"
            android:contentDescription="Crayon"
            android:layout_margin="8dp" />

        <ImageButton
            android:id="@+id/fork_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_fork"
            android:background="@drawable/tool_selector"
            android:contentDescription="Fourchette"
            android:layout_margin="8dp" />

        <ImageButton
            android:id="@+id/rectangle_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_rectangle"
            android:background="@drawable/tool_selector"
            android:contentDescription="Rectangle"
            android:layout_margin="8dp" />
        <ImageButton
            android:id="@+id/eraser_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/eraser"
            android:background="@drawable/tool_selector"
            android:contentDescription="Gomme"
            android:layout_margin="8dp" />
        <ImageButton
            android:id="@+id/discard_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@android:drawable/ic_menu_delete"
            android:background="@drawable/tool_selector"
            android:contentDescription="Gomme"
            android:layout_margin="8dp" />

    </LinearLayout>

    <com.example.microsoftpaint.DrawingView
        android:id="@+id/drawing_view"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@android:color/white"
        android:layout_margin="16dp"
        app:layout_constraintTop_toBottomOf="@id/tools_layout"
        app:layout_constraintBottom_toTopOf="@id/colors_layout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <LinearLayout
        android:id="@+id/colors_layout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/language_spinner">

        <Button
            android:id="@+id/color_red"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@android:color/holo_red_dark"
            android:layout_margin="8dp" />

        <ImageButton
            android:id="@+id/color_picker"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@android:drawable/ic_menu_more"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Plus de couleurs"
            android:layout_margin="8dp" />

        <ImageButton
            android:id="@+id/save_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@android:drawable/ic_menu_save"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Sauvegarder"
            android:layout_margin="8dp" />

    </LinearLayout>

    <Spinner
        android:id="@+id/language_spinner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        app:layout_constraintBottom_toBottomOf="@id/colors_layout"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/colors_layout" />

</androidx.constraintlayout.widget.ConstraintLayout>